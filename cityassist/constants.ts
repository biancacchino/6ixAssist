import { Coordinate } from './types';

export const DEFAULT_CENTER: Coordinate = { lat: 43.6532, lng: -79.3832 }; // Toronto City Hall

// All resources are now fetched dynamically from real data sources
// See services/establishmentService.ts for data fetching